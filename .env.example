# Payload CMS Configuration
PAYLOAD_SECRET=your-super-secret-key-here
DATABASE_URI=mongodb+srv://username:password@cluster.mongodb.net/cplab?retryWrites=true&w=majority

# Next.js Configuration
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# ========================================
# SMTP Email Configuration (Resend)
# ========================================
# Get API key from: https://resend.com/api-keys
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASS=re_your_resend_api_key_here
SMTP_FROM_NAME=CPLab
SMTP_FROM_EMAIL=noreply@yourdomain.com

RESEND_API_KEY=put-your-resend-api-key-here
RESEND_AUDIENCE_ID=your-audience-id-here

# ========================================
# Cloudflare R2 Storage Configuration
# ========================================
# Get credentials from: https://dash.cloudflare.com → R2 → Manage R2 API Tokens
# IMPORTANT: 
# 1. Create an R2 bucket first
# 2. Create API tokens with Object Read & Write permissions
# 3. Set up a public domain for the bucket (optional but recommended)
# 4. All media uploads will be stored in R2, not locally
R2_ACCOUNT_ID=your_32_character_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=cplab-media
R2_PUBLIC_URL=https://your-r2-bucket.domain.com

# Optional: For production deployments
# NODE_ENV=production
# PAYLOAD_PUBLIC_SERVER_URL=https://your-domain.com
