name: Publish Docker image for frontend

on:
  push:
    branches:
      - 'main'

jobs:
  build:
    name: Build and publish the frontend image
    runs-on: ubuntu-latest

    permissions:  # 👈 IMPORTANT: enable correct permissions for GITHUB_TOKEN
      contents: read
      packages: write

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and publish the API image
        run: |
          docker build -t ghcr.io/cp-laboratory/cplab-latest:latest .
          docker push ghcr.io/cp-laboratory/cplab-latest:latest

    #   - name: Trigger redeployment
    #     run: |
    #       curl https://dokploy.caardify.com/api/deploy/wUsSLqlVJC5zCNF_0L8Jf